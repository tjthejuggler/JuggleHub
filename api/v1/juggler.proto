syntax = "proto3";
package juggler.v1;

// 3D vector for positions, velocities, accelerations
message Vector3 {
  double x = 1;
  double y = 2;
  double z = 3;
}

// 2D vector for pixel coordinates
message Vector2 {
  double x = 1;
  double y = 2;
}

// Color information in BGR format
message Color {
  uint32 b = 1;  // Blue component (0-255)
  uint32 g = 2;  // Green component (0-255)
  uint32 r = 3;  // Red component (0-255)
}

// HSV color range for ball detection
message HSVRange {
  Vector3 min_hsv = 1;  // Minimum HSV values
  Vector3 max_hsv = 2;  // Maximum HSV values
}

// Ball detection and tracking information
// 2D bounding box for video overlay
message BoundingBox2D {
  float x = 1;
  float y = 2;
  float width = 3;
  float height = 4;
  float confidence = 5; // Add confidence score
  int32 class_id = 6;
  string class_name = 7;
}

message Ball {
  int32 id = 1; // The raw bytetrack ID, can change. May be -1 if predicted.
  Vector3 position = 2;
  BoundingBox2D bounding_box_2d = 3; // For video overlay
  string class_name = 4;

  enum Status {
    TRACKED = 0;   // Directly observed in this frame
    PREDICTED = 1; // Position is a physics-based prediction
    OCCLUDED = 2;  // Assumed to be held in a hand
  }
  Status status = 5;

  Vector2 projected_pos_2d = 6;
  int32 logical_id = 7; // The persistent, logical ID of the ball (e.g., 0, 1, 2)
  float distance_to_nearest_wrist = 8; // Distance to nearest wrist in meters (-1 if no hands visible)
}

// Color-tracked ball (new simplified tracking system)
message ColorTrackedBall {
  int32 logical_id = 1;           // Persistent ID (0, 1, 2)
  string color_name = 2;          // Associated color profile name
  Vector2 pixel_pos = 3;          // Current 2D position
  Vector3 world_pos = 4;          // Current 3D position
  bool is_active = 5;             // Whether this tracker is currently tracking
  int32 associated_wrist_id = 6;  // -1 if not associated, 0=left, 1=right if associated
  int32 frames_since_seen = 7;    // Frames without detection
}

// Hand position and tracking
message KeyPoint {
  Vector2 pos_2d = 1;
  Vector3 pos_3d = 2;
  float confidence = 3;
}

message Hand {
  int32 id = 1;                     // 0 for left, 1 for right
  Vector2 position_2d = 2;          // 2D pixel position of the wrist
  Vector3 wrist_pos_3d = 3;         // 3D position of the wrist
  double confidence = 4;            // Detection confidence (0.0-1.0)
  bool is_visible = 5;              // Whether hand is visible
  repeated KeyPoint keypoints = 6;  // All keypoints for the hand
  string side = 7;                  // "left" or "right" for compatibility
  Vector3 position_3d = 8;          // Alias for wrist_pos_3d for compatibility
}

// IMU data from smartwatches
message IMUData {
  string watch_name = 1;            // Watch identifier
  string watch_ip = 2;              // Watch IP address
  Vector3 acceleration = 3;         // Acceleration in m/s²
  Vector3 gyroscope = 4;            // Angular velocity in rad/s
  Vector3 magnetometer = 5;         // Magnetic field (if available)
  double accel_magnitude = 6;       // Acceleration magnitude
  double gyro_magnitude = 7;        // Gyroscope magnitude
  uint64 timestamp_us = 8;          // Timestamp in microseconds
  double data_age_ms = 9;           // Age of data in milliseconds
}

// Throw and catch events for apps
message ThrowCatchEvent {
  enum EventType {
    THROW = 0;
    CATCH = 1;
  }
  
  EventType type = 1;               // Type of event
  int32 ball_id = 2;                // Logical ID of the ball
  int32 hand_id = 3;                // Hand ID (0=left, 1=right)
  uint64 timestamp_us = 4;          // Event timestamp
  Vector3 position = 5;             // 3D position where event occurred
  float confidence = 6;             // Event confidence score
}

// Camera intrinsics for 3D calculations
message CameraIntrinsics {
  double fx = 1;                    // Focal length X
  double fy = 2;                    // Focal length Y
  double ppx = 3;                   // Principal point X
  double ppy = 4;                   // Principal point Y
  double depth_scale = 5;           // Depth scale factor
}

// System status and diagnostics
message SystemStatus {
  bool camera_connected = 1;        // Camera connection status
  bool engine_running = 2;          // Engine process status
  double fps = 3;                   // Current FPS
  uint32 frame_count = 4;           // Total frames processed
  string mode = 5;                  // Current mode (live, playback, etc.)
  string error_message = 6;         // Error message if any
  uint64 timestamp_us = 7;          // Status timestamp
}

// STEP 2: Kalman Predictions - Predicted positions before detection
message KalmanPrediction {
  int32 logical_id = 1;             // Tracker ID
  Vector3 predicted_pos = 2;        // Predicted 3D position
  Vector2 predicted_pos_2d = 3;     // Projected 2D position
  bool is_in_freefall = 4;          // Whether gravity is being applied
}

// STEP 4: Filtered/Rejected Detections - Detections that failed depth filtering
message FilteredDetection {
  BoundingBox2D box = 1;            // Original bounding box
  string reason = 2;                // Reason for filtering (e.g., "depth_out_of_range", "duplicate")
  float depth_value = 3;            // Depth value that caused filtering
}

// STEP 5: 3D Distance Association - Shows matching between trackers and detections
message TrackerAssociation {
  int32 tracker_id = 1;             // Tracker logical ID
  int32 detection_index = 2;        // Index into raw_detections
  float distance_3d = 3;            // 3D distance used for matching
  Vector3 tracker_pos = 4;          // Tracker predicted position
  Vector3 detection_pos = 5;        // Detection 3D position
}

// STEP 6: Auto-Initialization - Newly initialized trackers
message NewTracker {
  int32 logical_id = 1;             // New tracker ID
  Vector3 initial_pos = 2;          // Initial 3D position
  Vector2 initial_pos_2d = 3;       // Initial 2D position
  int32 detection_index = 4;        // Which detection initialized this tracker
}

// STEP 8: Throw/Catch State - Current state of each ball
message BallState {
  int32 logical_id = 1;             // Ball ID
  enum State {
    IN_FLIGHT = 0;
    TRANSITIONING = 1;
    HELD = 2;
  }
  State state = 2;                  // Current state
  int32 associated_hand_id = 3;     // Hand ID if held/transitioning (-1 if in flight)
  float confidence = 4;             // State confidence score
  int32 frames_in_state = 5;        // How many frames in current state
}

// STEP 9: Legacy Occlusion - Occlusion states
message OcclusionState {
  int32 logical_id = 1;             // Ball ID
  bool is_occluded = 2;             // Whether ball is occluded
  int32 occluding_hand_id = 3;      // Which hand is occluding (-1 if not occluded)
  float distance_to_hand = 4;       // Distance to occluding hand
}

// STEP 11: Color Tracking Search - Shows search regions and found blobs
message ColorSearchRegion {
  int32 logical_id = 1;             // Ball ID
  string color_name = 2;            // Color being searched for
  Vector2 search_center = 3;        // Center of search region
  float search_radius = 4;          // Search radius in pixels
  Vector2 blob_center = 5;          // Found blob center (0,0 if not found)
  float blob_area = 6;              // Blob area in pixels
  bool found = 7;                   // Whether blob was found
}

// Main frame data packet sent from engine to hub
message FrameData {
  uint64 timestamp_us = 1;          // Frame timestamp in microseconds
  repeated Ball balls = 2;          // Detected and tracked balls
  repeated Hand hands = 3;          // Detected hands
  repeated IMUData imu_data = 4;    // IMU data from watches
  CameraIntrinsics intrinsics = 5;  // Camera intrinsics
  SystemStatus status = 6;          // System status
  
  // Frame metadata
  uint32 frame_width = 7;           // Frame width in pixels
  uint32 frame_height = 8;          // Frame height in pixels
  uint32 frame_number = 9;          // Sequential frame number
  
  // Optional image data (base64 encoded for streaming)
  repeated BoundingBox2D raw_detections = 10; // All raw YOLO detections (STEP 3)
  repeated BoundingBox2D unmatched_detections = 16; // Detections that passed filtering but weren't matched (STEP 13)
  bytes color_image_b64 = 11;      // Base64 encoded color image
  bytes depth_image_b64 = 12;      // Base64 encoded depth image
  bool ir_projector_active = 13;   // Status of the IR projector
  repeated ColorTrackedBall color_tracked_balls = 14; // New simplified color tracking (STEP 11)
  repeated ThrowCatchEvent throw_catch_events = 15; // Throw and catch events for apps
  
  // NEW: Visualization data for pipeline steps
  repeated KalmanPrediction kalman_predictions = 17;      // STEP 2: Predicted positions
  repeated FilteredDetection filtered_detections = 18;    // STEP 4: Rejected detections
  repeated TrackerAssociation tracker_associations = 19;  // STEP 5: Matching visualization
  repeated NewTracker new_trackers = 20;                  // STEP 6: Auto-initialized trackers
  repeated BallState ball_states = 21;                    // STEP 8: Throw/catch states
  repeated OcclusionState occlusion_states = 22;          // STEP 9: Occlusion info
  repeated ColorSearchRegion color_search_regions = 23;   // STEP 11: Color tracking search
}

// Command and control API for managing engine modules
message CommandRequest {
  enum CommandType {
    UNKNOWN = 0;
    LOAD_MODULE = 1;
    UNLOAD_MODULE = 2;
    SEND_COLOR_COMMAND = 3;
    CONFIGURE_MODULE = 4; // New command type for module-specific configuration
    RECORD_START = 5;
    RECORD_CONTINUOUS_START = 6;
    RECORD_CONTINUOUS_STOP = 7;
    RESTART_WITH_CAMERA_SETTINGS = 8; // New command for camera settings
    CAMERA_STOP = 9; // Stop camera feed
    CAMERA_START = 10; // Start camera feed with settings
    CALIBRATE_OBJECT = 11; // Force-set the position of a logical tracker
    SET_POSE_MODEL_ENABLED = 12; // Enable or disable the pose model
    CALIBRATE_COLOR = 13; // Calibrate a color profile by clicking on a ball
    ENABLE_FEATURE = 14; // Enable an engine feature for apps
    DISABLE_FEATURE = 15; // Disable an engine feature for apps
  }
  
  CommandType type = 1;
  string module_name = 2;           // Name of the module to load/unload
  uint64 timestamp_us = 3;          // Command timestamp
  ColorCommand color_command = 4;
  map<string, string> module_args = 5; // Arguments for the module
  string camera_settings_file = 6;  // Path to camera settings JSON file
  uint32 camera_width = 7;          // Camera resolution width
  uint32 camera_height = 8;         // Camera resolution height
  uint32 camera_fps = 9;            // Camera frame rate
  
  // For CALIBRATE_OBJECT
  int32 logical_id_to_calibrate = 10;
  Vector2 calibration_pixel_pos = 11;
  bool record_with_yolo_boxes = 12;
  bool record_with_bytetrack_boxes = 13;
  bool pose_model_enabled = 14;
  
  // Visualization states for recording
  VisualizationStates visualization_states = 19;
  
  // For CALIBRATE_COLOR
  string color_name = 15;           // Name of the color profile to calibrate
  int32 click_x = 16;               // X coordinate of the click
  int32 click_y = 17;               // Y coordinate of the click
  
  // For ENABLE_FEATURE / DISABLE_FEATURE
  string feature_name = 18;         // Name of the feature to enable/disable
}

// Visualization states for recording with overlays
message VisualizationStates {
  bool show_kalman_predictions = 1;
  bool show_raw_detections = 2;
  bool show_filtered_detections = 3;
  bool show_associations = 4;
  bool show_new_trackers = 5;
  bool show_hand_tracking = 6;
  bool show_ball_states = 7;
  bool show_occlusion = 8;
  bool show_skeleton = 9;
  bool show_color_search = 10;
  bool show_color_tracker = 11;
  bool show_tracked_boxes = 12;
  bool show_unmatched_detections = 13;
  bool show_tails = 14;
}

message ColorCommand {
  string ball_id = 1;
  Color color = 2;
}

message CommandResponse {
  bool success = 1;                 // Whether the command was successful
  string message = 2;               // Response message (e.g., error details)
  uint64 timestamp_us = 3;          // Response timestamp
}